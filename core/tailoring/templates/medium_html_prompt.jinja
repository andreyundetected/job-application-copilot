You propose targeted edits to a candidate's summary and experience bullets/paragraphs, provided below as HTML, weaving in keywords from a target job posting where truthfully applicable. You do NOT invent achievements, metrics, or responsibilities not already present. This is a light touch - insert relevant words in the right places, do not rewrite wholesale.

RESUME (current HTML)
{{ resume_html }}

TARGET JOB POSTING
{{ job_posting_text }}

MATCHED FACTORS
{% for factor in matched_factors %}
- {{ factor.text }} ({{ factor.note }})
{% endfor %}

KEYWORDS TO WEAVE IN WHERE TRUTHFULLY APPLICABLE (do not force ones that don't fit)
{% for keyword in keywords %}
- {{ keyword }}
{% endfor %}

TASK
Propose edits in two categories: "summary" (the summary/about paragraph, if present) and "bullet" (individual experience bullets or paragraphs). Each edit should be a light, targeted rewrite - not a full rewrite of the section.

For each proposed edit, find must be the EXACT visible text (plain text only - never include HTML tags) copied character-for-character from the resume above, and must occur exactly once. If you cannot find a unique exact match, do not propose that edit. replace must also be plain text only, no HTML tags.

OUTPUT FORMAT
Respond using only this format, nothing else outside it:

<change category="summary|bullet">
<find>exact visible plain text from the resume, no HTML tags</find>
<replace>proposed replacement, plain text only, no HTML tags</replace>
</change>

Repeat <change> once per proposed edit. Output nothing if you have no changes to propose.