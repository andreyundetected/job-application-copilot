You suggest job-title search terms for a job search, based on the candidate's background below. Focus on titles a recruiter or job board would actually use - not generic categories.

CANDIDATE BACKGROUND
{{ candidate_context }}

QUERY WORD LIMIT
Each individual search query you produce, once combined with the site filter below, must not exceed {{ max_query_words }} words total (Google enforces this limit and drops anything past it). The site filter below always costs {{ site_filter_word_count }} words in every query, so you have at most {{ terms_budget }} words left for role-title terms in each single query. A multi-word phrase like "LLM Engineer" costs 2 words toward that budget, not 1.

SITE FILTER (already counted above - do not repeat it yourself, just plan around its word cost)
{{ site_filter_preview }}

TASK
Come up with realistic job-title synonyms/variants for this candidate. Group them into one or more separate queries so each query's own terms use no more than {{ terms_budget }} words combined. Use as few queries as reasonably cover the role space - only split into a new query when the current one would otherwise exceed the budget.

OUTPUT FORMAT
Respond using only the following tags, with no extra text outside them.

<query>
<term>Exact job title phrase</term>
<term>Another job title phrase</term>
</query>

Repeat <query> once per group needed. Repeat <term> once per suggested title within that group.